### roboconnection-example

A simple example for testing the roboconnection library used to establishing ROM (remote operation mode) connections with robots like Jibo.

#### run
```
yarn
yarn build
yarn start
```
